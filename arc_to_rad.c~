#include "maze.h"
/**
 *arcToRad: converts angle to radians
 *
 *@arcAngle: angle to be converted
 *
 *Return: angle converted to radian
 */

float arcToRad(float arcAngle)
{
float pi = (22 / 7);
return((float)((arcAngle * pi) / 180));
}
